/* If the total selling price of 15 items and the total profit earned
on them is input through the keyboard, write a program to find the
cost price of one item. */

#include<stdio.h>
int main()
{
	int sp,tp,cp;
	printf("Enter the total selling price: ");
	scanf("%d", &sp);
	
	printf("Enter the total profit: ");
	scanf("%d", &tp);
	
	cp=sp-tp;
	printf("Cost price of one item is %d", cp/15);
	return 0;
}	
 
