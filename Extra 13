/*If a five-digit number is input through the keyboard, write a programto print a new number by 
adding one to each of its digits. For example if the number that is input is 12391 then the output 
should be displayed as 23402.*/

#include<stdio.h>
int main()
{
	int num,temp,rem,res;
	printf("Enter a five digit number: ");
	scanf("%d", &num);
	
	temp=num;
	rem=temp%10;
	if(rem==9)
		res=0;
	else
		res=rem+1;	
	
	rem=(temp/10)%10;
	if(rem==9)
		res=res;
	else
		res=res+((rem+1)*10);
	
	rem=(temp/100)%10;
	if(rem==9)
		rem=res;
	else		
		res=res+((rem+1)*100);
	
	rem=(temp/1000)%10;
	if(rem==9)
		res=res;
	else
		res=res+((rem+1)*1000);
	
	rem=(temp/10000)%10;
	if(rem==9)
		res=res;
	else
		res=res+((rem+1)*10000);
	
	printf("Result is : %d", res);
	return 0;
}	
