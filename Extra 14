/* Write a program in which a character is input through the keyboard, corresponding ASCII will be shown as result*/

#include<stdio.h>
int main()
{
	char al;
	printf("Enter any character: ");
	scanf("%c", &al);
	
	printf("ASCII value coresponding to entered character: %d",al);
	return 0;
}	
